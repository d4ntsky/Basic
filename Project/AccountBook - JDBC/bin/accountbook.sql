
DROP TABLE ACCOUNTBOOK
CASCADE CONSTRAINTS;

DROP SEQUENCE SEQ_BOOK;

CREATE TABLE ACCOUNTBOOK(
	SEQ NUMBER(8),
	ID VARCHAR2(30),		-- 외래키
	IO_KIND VARCHAR2(1),	-- 수입/지출 구분
	AMOUNT	NUMBER(9),		-- 금액
	CONTENT VARCHAR2(200),	-- 내용
	WDATE DATE				-- 입력일		SYSDATE
);

-- SEQUENCE
CREATE SEQUENCE SEQ_BOOK
START WITH 1
INCREMENT BY 1;

-- 외래키(ID)
ALTER TABLE ACCOUNTBOOK
ADD CONSTRAINT FK_BOOK FOREIGN KEY(ID)
REFERENCES MEMBER(ID);

select * from ACCOUNTBOOK;

SELECT IO_KIND, AMOUNT, CONTENT, WDATE  
FROM ACCOUNTBOOK
WHERE WDATE BETWEEN TO_DATE('200701', 'YYMMDD') 
	AND TO_DATE('200702', 'YYMMDD')
	AND ID = 'aaa';














